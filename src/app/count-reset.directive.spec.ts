import { CountResetDirective } from './count-reset.directive';

describe('CountResetDirective', () => {
  it('should create an instance', () => {
    const directive = new CountResetDirective();
    expect(directive).toBeTruthy();
  });
});
